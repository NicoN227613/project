{% extends 'admin/base.html.twig'%}

{% block body %}
<div class="dashboard__title">
    {{ icon('pen') }}
    Edition d'un produit
</div>

<div class="dashboard__card">
    {{ include('product/_form.html.twig', {form: form, action: 'product.action.edit' | trans }) }}
</div>
{% endblock %}