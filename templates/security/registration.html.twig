{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form)}}
    {{ form_row(form.email)}}
    {{ form_row(form.password)}}
    <button type="submit" class="btn btn__text">S'incrire</button>
    {{ form_end(form)}}
{% endblock %}