{{ form_start(form, { 'attr' : { 'class' : 'form' }} ) }}

<h1>Modifier</h1>

{{ form_errors(form.pseudo) }}
<div class="form__group">
    <label for="inputEmail">Pseudo</label>
    {{ form_widget(form.pseudo, { 'attr' : { 'class' : 'form__control'}})}}
</div>

{{ form_errors(form.email) }}
<div class="form__group">
    <label for="inputEmail">Email</label>
    {{ form_widget(form.email, { 'attr' : { 'class' : 'form__control'}})}}
</div>

{{ form_errors(form.image.file) }}
<div class="">
    <label for="inputImage">Télécharger une image</label>
    {{ form_widget(form.image.file, { 'attr' : { 'class' : 'form__control'} } ) }}
</div> 

<a href=" {{ path('user_user_password') }} ">Cliquez ici pour changer de mot de passe</a>

<button type="submit" class="btn__submit">{{ action }}</button>
{{ form_end(form)}}